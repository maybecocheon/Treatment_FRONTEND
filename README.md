# 2026/01/28
- 대시보드 형식 수정
  > 대시보드 메뉴는 좌측이 아닌 상단에 있는 것이 자연스럽다고 해서 수정함

# 2026/01/29
- 글래스모피즘으로 디자인 통일

# 2026/01/30
- 회원 정보 수정 및 탈퇴 페이지 추가
  > 기본적인 CRUD 구현 위함

# 2026/02/02
- 컴포넌트 추출
  > input, button, select 등 여러 번 활용되는 컴포넌트 추출
- 모달 page.tsx
  ~~> 배수지 id를 이용해 모달 열 때나 카테고리 선택 시 path 변경되도록 함 => 2026/02/05 뒤로 가기 버튼 누르거나 했을 때 바로 이전 페이지를 볼 수 있도록 "[id]" 페이지로 이동해서 모달 열리도록 함~~ (2026/02/13)
- 배수지 수요예측 그래프 (백엔드 api 호출)

# 2026/02/03
- 메인 대시보드 추가
- 시설명 불러오기 (백엔드 api 호출)

# 2026/02/04
- 커스텀 훅 만들기
  > 백엔드 api로 호출한 데이터 atom으로 관리하고 훅에서 load 및 reset까지 => 2026/02/05 atom으로 관리하면 매번 reset 해 줘야 해서 각 페이지마다 다르게 불러올 수 있도록 훅 안에서 state로 관리함 

# 2026/02/05
- 백엔드와 연결해서 로그인, 회원가입, 회원 정보 수정, 비밀번호 변경 구현
- 커스텀 fetch Wrapper
  > 로그인 후 백엔드에 쿠키 전달하기 위해 fetch 할 때 credentials: "include" 함
  > 로그인 후에 이용 가능한 사이트이기 때문에 401(권한 없음) 에러 뜰 때 "/" 랜딩 페이지로 돌아가도록 함
- 그래프 디자인 통일

# 2026/02/06
- sonner 라이브러리 사용
  > 행위의 성패를 알려 주는 알림창을 sonner 라이브러리로 커스텀함
- 401 에러 
  > 리프레시 토큰 갱신 후 이용 가능하도록 함
  > 실패하면 "/" 랜딩 페이지로 돌아가도록 함
  > 랜딩 페이지로 돌아간 뒤 로그인하면 원래 있던 페이지로 돌아갈 수 있도록 함 (useSearchParams 사용)
- api 호출 시 loading 사용
  > api 여러 번 호출되는 것 방지

# 2026/02/09
- response 에러 시 백엔드로부터 message 받아 옴
  > response.json()으로 보내 준 message 속성 활용
- baseUrl 수정
  > /api/proxy로 호출하던 것에서 .env.local에서 baseUrl 불러와서 api 호출하는 것으로 수정
  > 백엔드와 프론트엔드는 다른 도메인. 쿠키 잘 주고받을 수 있도록 백엔드 측에서 https로 주소 변경 후 쿠키 옵션 수정함.
- 모바일 화면으로 전환 시 footer와 main이 달라붙는 문제 해결
  > 맨 아래 요소에 mb-4 md:mb-0 줌
- 각 main 페이지 한눈에 보이게 하기
  > h-screen 아닌 h-full 사용

# 2026/02/10
- refresh token 오류 
  > 401 에러 시 토큰 새로 발급받아야 하는데 그러지 않고 랜딩 페이지로 이동도 안 함 => 백엔드 측에서 SameSite=None 안 한 게 원인이라 수정 부탁드림
- 스크롤바 커스텀
  > 라이브러리 사용하려고 했으나 라이브러리 사용 시 컴포넌트마다 스크롤바 라이브러리를 씌워 줘야 한다고 해서 global.css에서 스크롤바 전역 커스텀함

# 2026/02/11
- 스켈레톤 수정
  > 원래는 모든 스켈레톤을 페이지 단위로 생성했으나 사용자의 편의성을 높이기 위해 페이지 단위, 컴포넌트 단위로 나누어 스켈레톤을 생성함
  > 컴포넌트 단위: 차트, 맵, 카드, 셀렉트 박스 등
  > 페이지 단위: 모달(상세 페이지) 등
- api 호출 에러 시
  > 에러 메시지 및 다시 시도 버튼 제시
- 대시보드 변경
  > 평균 수위, 최저 수위, 최고 수위보다는 모든 배수지의 현재 수위를 모니터링 할 수 있는 수위 현황 그림

# 2026/02/12
- 스케줄링 페이지 변경
  > 스케줄링 설명 영역과 분석 영역으로 나뉨
  > 대시보드에 스케줄링 추가하는 Dashboard2 컴포넌트도 만들어 둔 상태
- 히스토리 페이지 변경

# 2026/02/13
- 수위 현황 물 흐르는 모션
  > requestAnimationFrame(줄여서 rAF): 웹에서 부드러운 애니메이션이나 끊김 없는 리사이즈 처리를 구현할 때 쓰는 함수
- 대시보드에서 상세 분석 속 차트 클릭하면 모달 팝업

# 2026/02/19
- React createPortal
  ~~> 모달 코드를 DOM 구조의 최상단(body 바로 아래)으로 강제 이동시키는 Portal 기능을 사용하여 모달이 항상 맨앞에 뜨도록 함~~(2026/02/20)
- .toLocaleString(undefined, { maximumFractionDigits: 0 })
  > toFixed와 toLocaleString 혼용 어려워서 찾아보니 toLocaleString에 연관된 속성이 있다는 것을 발견
- 의존성 변경 시 데이터 자동 로드
  > 원래 페이지마다 데이터를 로드할 수 있도록 했는데 커스텀 훅 안에서 의존성 변경 시 데이터가 자동 로드될 수 있도록 함으로써 코드를 줄임

# 2026/02/20
- 맵 페이지 수정
  > 지도 페이지 가득 채우고 오버레이 선택 시 모달 대신 사이드바 형식으로 상세 정보 드러나게 함
- 스케줄링 페이지 수정
  > 구체적인 펌프 운영 방법에 대한 내용 추가

# 2026/02/23
- 맵: 배수지 모달 및 사이드바 배경색 변경
- 히스토리 뷰어: 한 달치와 특정 일자 둘 다 보여 주기
  > 하루 총 송수량, 하루 평균 압력을 바탕으로 한 달치 보여 주기
- 예측 차트 데이터
  > isProcessing = true일 때 데이터를 다시 fetch 해 오도록 함
  > 15분마다 한 번씩 자동 fetch 해 오도록 함

# 2026/02/24
- 리액트 쿼리
  > useQuery 활용해서 15분마다 데이터 갱신하도록 함
  > 리액트 쿼리는 데이터를 낡은 데이터와 신선한 데이터로 구분함. 15분 동안은 모든 데이터를 신선한 데이터로 두어서 새로고침 하지 않는 이상 새로운 데이터 불러오지 않도록 해 체감 속도 높임.
  > 쿼리 키로써 데이터를 구분함

# 2026/02/25
- 대시보드 수위 현황 물 흐름 선 수정
- 모든 api 연결 완료

# 2026/02/26
- next themes
  > 다크모드 지원
- 최적화 스케줄링 클릭 시 모달 오픈 기능 구현
- 수위 패널에 타임 슬라이드 기능 추가해 동적인 느낌 구현